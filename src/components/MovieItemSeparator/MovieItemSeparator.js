import React from 'react';
import {View} from 'react-native';
import styles from './MovieItemSeparatorStyles';

const MovieItemSeparator = () => {
  return <View style={styles.separator} />;
};

export default MovieItemSeparator;
